ToDo:
	Nick:
		lightning storms are still noticeable in Precipitation
		Time transitions from levels with no time setting causes transition bugs between levels.
			suspect this has to do with long to int casting and visa versa. also noticed the server time and 
			player time are offset after a player.setPlayerTime even though the parameter is the server time. 
		flat terrain finder for placing mirages.
		config options for server time set, and weather set. 
		