ToDo:
	Nick:
		lightning storms are still noticeable in Precipitation
		flat terrain finder for placing mirages.
		config options for server time set, and weather set. 
		efficiency distance check
		ghast spawn
		delete beacons
		beacon creation distance limit
		torches that change level
		chunk player = null errors
		entity burning in light
		potion effects negated on level change
		
		2.0
		playerData is probably still used for beacon point stuff in various places (modifying a player's points)
			sync playerData and DarkPlayer, specifically points
		find out how to send the weather packet
		correct config values onEnable.
		
		